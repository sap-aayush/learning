alert("hello this code works")

let a = prompt("enter a") // prompt takes the value of a
document.write(a)

// by the above code the number is stored in the string and if u want number then parse

let write = confirm("do u want to wrtite")  // ask yes or no

if (write) {
    document.write(a)
} else {
    document.write("allow me to write")
}


// these three give old look so generally don't prefer it for the frontend websites rather use the modal