let a = 1
let b = 3
let c = 5
let d = 8


// in the syntax only in place of the return data type just write the term function and in the parameters no need to put data types

function avergae(a,b,c,d) {
    return (a+b+c+d)/4
}

// not stressing too much as evth same as

// also can be like this
const sum = (p,q)=>{   // by the arrrows ... simply
    return p+q
}

console.log(sum(9,8))

const hello = ()=>{
    console.log("hello! sap here...")
}

hello();